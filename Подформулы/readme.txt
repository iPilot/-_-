Файл data.txt содержит данные о наборах разрешенных символов:
1-а¤ строка - алфавит символов используемых в именах переменных(a..zA..Z по-умолчанию)
2-а¤ строка - бинарные операции, разделенные пробелами на группы по приоритетам в порядке его уменьшения
3-¤ строка - унарные операции(обладают наивысшим приоритетом)

Ввиду обработки приоритета операций запись вида (~a) не будет считать правильной формулой. Это можно было сделать,
но нужно всю структуру менять и отслеживать глубину вложенности скобок. Может быть, потом я сделаю это.